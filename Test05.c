#include <stdio.h>
#include <math.h>
#include <string.h>

int main()




{
        char seq[ 1000 ];
        int length;
        int i;


        while( scanf("%s", seq ) == 1)
        {
                length = strlen (seq);
                printf("The lenght of the seq is %d\n", length);
        }
