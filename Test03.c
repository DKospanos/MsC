int main()
{
        char seq[ 1000 ];
        int length;

        scanf("%s", seq );
        length = strlen (seq);
        {
                printf("The lenght of the seq is %d\n", length);
        }
}
